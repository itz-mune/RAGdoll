# Python / uv
src-tauri/sidecar-src/.venv/
src-tauri/sidecar-src/__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Large base ONNX model — only the quantized version (22 MB) is committed
src-tauri/sidecar-src/models/all-MiniLM-L6-v2-onnx/model.onnx

# Node
node_modules/

# Tauri build output
src-tauri/target/

# Local uv binary (placed by CI for release builds; download manually for dev)
src-tauri/binaries/

# Env files
.env
.env.local

# LanceDB vector data
src-tauri/sidecar-src/memory/data/
src-tauri/sidecar-src/*.db

# OS
.DS_Store
Thumbs.db

# Local/editor config
.vscode/
.claude/
.mcp.json
mcp.json

# Community plugin repos live separately (root-level only, not sidecar/plugins/)
/plugins/

# Separate plugins repo (own git repo — pushed independently)
plugins-repo/

# Runtime directories created by the sidecar at startup
ragdoll_plugins/
ragdoll_memory/
src-tauri/sidecar-src/ragdoll_memory/

# Local notes
INSTALLATION_COMMANDS.md
PROVIDER_EXTENSION_SUMMARY.md
tailwindd.cconfig.js
tsconfig.js
